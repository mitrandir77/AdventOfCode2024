// Advent of Code 2024
// (c) 2023 Mateusz Kwapich

use anyhow::Result;
use std::io::BufRead;

fn main() -> Result<()> {
    let stdin = std::io::stdin();

    for _line in stdin.lock().lines() {}
    Ok(())
}
